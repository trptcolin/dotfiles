#!/usr/bin/env bash

set -e

# Basics
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
brew tap homebrew/cask-versions
brew tap homebrew/cask-drivers
brew install --cask 1password yubico-authenticator
